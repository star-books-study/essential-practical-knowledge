# 7장. IO 병목, 어떻게 해결하지
## 네트워크 IO와 자원 효율
- 서버는 네트워크 통신을 기반으로 동작
- 네트워크를 통해 데이터를 주고 받는 과정

```java
outputStream.write(...); // 출력 스트림으로 데이터 보내기
inputStream.read(...); // 입력 스트림으로 데이터 받기
```
- 입출력(IO)를 하는 동안 스레드는 대기한다
- 입출력에 소요되는 시간은 코드를 실행하는 시간보다 훨씬 길다

> 블로킹 : 작업이 완료될 때까지 스레드가 대기하는 것

- IO 대기에 따른 CPU 낭비를 막는 법 : 스레드를 많이 만들기
- 그러나 스레드를 만드는 데도 한계가 있음 스레드는 수백 KB에서 수 MB를 사용함
- 컨텍스트 스위칭에도 시간이 걸림
- ㅅㅓ버 성능을 늘리는 다른 방법은 자원 효율을 늘리는 것

## 가상 스레드로 자원 효율 높이기
- 특별한 노력 없이 CPU 효율 높이는 방법 : 자바의 가상 스레드 혹은 Go의 고루틴
    - 둘 다 **경량 스레드**
        - OS가 관리하는 스레드가 아니라 JVM 같은 언어의 **런타임이 관리**하는 스레드
        - OS가 CPU로 실행할 스레드를 스케줄링하듯, 언어 런타임이 OS 스레드로 실행할 경량 스레드를 스케줄링 한다
- Jvm은 플랫폼 스레드로 구성된 풀을 유지
- CPU가 OS 스케줄러에 의해 여러 스레드를 번갈아 실행하는 것처럼 플랫폼 스레드도 JVM 스케줄러에 의해 여러 가상 스레드를 번갈아 실행

- 가상 스레드 장점
    - 훨씬 적은 매모리 사용
    - 스레드 생성 시간 짧음

### 네트워크 IO와 가상 스레드
- 가상 스레드는 실행 과정에서 블로킹 되면 언마운트 되고 실행이 멈춘다
> 언마운트 : 가상 스레드가 캐리어 스레드(플랫폼 스레드)에서 언마운트 되면 스레드는 실행을 멈춘다

- 플랫폼 스레드는 블로킹 연산을 만나면 다른 가상 스레드를 찾아서 연결함

> 블로킹 연산에는 IO 기능, ReemtrantLock, Thread.sleep() 등이 포함됨

### 가상 스레드와 성능
- IO-bound 작업 (입출력 관련)과 CPU-bound 작업 중 가상 스레드는 어디에 더 효과적?
- IO-bound일 때 CPU 낭비 없이 효율적으로 가상 스레드 작업 가능
    - IO는 가상 스레드가 지원하는 블로킹 연산
- CPU-bound 작업애서는 성능 개선 효과 X
    - 블로킹 연산이 없으므로 계속 1개의 가상 스레드만 사용
    - 동시 실행 효과 x
- IO-bound 작업이라도, 플렛폼 스레드 개수보다 가상 스레드 개수가 많아야 효과를 볼 수 있다
    - 대부분이 IO 대기 상태에 있으므로
- 가상 스레드의 이점을 얻으려면 CPU 코어 수를 줄이거나 트래픽이 더 많아져야 한다


### 가상 스레드의 중요한 장점
- 기존 코드 크게 수정할 필요 없음
    - 많이 사용하는 프레임워크나 라이브러리가 이미 지원

## 논블로킹 IO로 성능 높이기
- 경량 스레드가 많아질수록 메모리를 더 많이 사용하고 스케줄링 하는 데 시간도 많이 걸림
- 사용자가 폭발적으로 증가하면 서버의 IO 구현 방식을 구조적으로 변경해야 한다 -> 논블로킹 IO

### 논블로킹 IO 동작 개요
- 논블로킹 IO는 입출력이 끝날 때까지 스레드가 대기하지 않는다
- channel.read() : 데이터를 읽을 때까지 대기하지 않는다
    - 읽을 데이터가 없으면 바로 0을 리턴
> 데이터를 읽을 때까지 대기하는 블로킹 IO와는 다르다

```java
// channel : SocketChannel, buffer : ByteBuffer
int byteReads = channel.read(buffer);
// 읽은 데이터가 없어도 다음 코드 계속 실행
```
- 데이터를 조회했다는 가정 하에 코드를 짤 수는 없음
- 대신에 루프 안에 조회를 반복 호출하고 데이터를 읽었을 때만 처리하는 방식으로 구현 가능
